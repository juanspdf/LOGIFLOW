spring:
  application:
    name: auth-service

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/logiflow_auth
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true
    open-in-view: false

  jackson:
    time-zone: America/Guayaquil

  mvc:
    problemdetails:
      enabled: true

  # Desactivar autoconfiguración de Spring Security (Kong maneja autenticación)
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

server:
  port: 8081


# JWT Configuration
# IMPORTANTE: La variable JWT_SECRET debe estar configurada en docker-compose.yml o en las variables de entorno
# No se proporciona valor por defecto para evitar el uso accidental de claves inseguras en producción
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000
  refresh-expiration: 604800000
  issuer: logiflow-auth-service

# Springdoc OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  show-actuator: true

# Logging
logging:
  level:
    root: INFO
    com.logiflow.authservice: DEBUG
    org.springframework.security: DEBUG
    org.springframework: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Application specific
application:
  name: LogiFlow Auth Service
  version: 1.0.0
  description: Authentication and Authorization Service
  security:
    allowed-origins: http://localhost:3000,http://localhost:4200
    jwt:
      token-prefix: Bearer
      header-string: Authorization